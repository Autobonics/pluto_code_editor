name: pluto_code_editor
description: A complete code editor with line number, syntax highlighting and an output window. It is made for python programming, specifically for bonicPython which runs inside Pluto (a modular robotic kit).
version: 0.1.1
email: shahirc72@gmail.com
homepage: https://github.com/Autobonics/pluto_code_editor

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_highlight: ^0.7.0
  highlight: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
